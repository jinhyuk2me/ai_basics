## TL;DR
- **그래프 이론**은 노드와 엣지로 구성된 관계형 데이터를 수학적으로 분석하는 분야로, GNN과 네트워크 과학의 이론적 기초다.
- 이 노트는 그래프 이론 전체 내용의 허브로, 기초 → 알고리즘 → 스펙트럴/GNN 순서로 학습하는 로드맵을 제공한다.
- 각 주제별 상세 노트로 연결하여 인접 행렬부터 Graph Neural Networks까지 체계적으로 이해할 수 있다.

---

## 학습 로드맵

| 순서 | 주제 | 핵심 질문 | 바로 가기 |
|------|------|-----------|--------------|
| 1 | 그래프 기초와 표현 | 그래프란? 인접 행렬 vs 인접 리스트? | [[그래프 기초와 표현]] |
| 2 | 그래프 알고리즘 | BFS, DFS, Dijkstra는 어떻게 작동? | [[그래프 알고리즘]] |
| 3 | 스펙트럴 이론과 GNN | Laplacian이란? GCN은 어떻게 학습? | [[스펙트럴 그래프 이론과 GNN]] |

---

## 1. 그래프 기초와 표현

### 핵심 개념
- **그래프 정의**: $G = (V, E)$ (노드 + 엣지)
- **그래프 종류**: 방향/무방향, 가중/비가중, 이분 그래프
- **표현 방식**: 인접 행렬, 인접 리스트, 엣지 리스트
- **기본 성질**: 차수, 경로, 연결성, 순환

### 왜 중요한가?
- 관계형 데이터의 자연스러운 표현
- 표현 방식에 따라 메모리/연산 효율 크게 달라짐
- 소셜 네트워크, 추천 시스템, 분자 구조 등 다양한 응용

### 상세 학습
👉 [[그래프 기초와 표현|그래프 기초 상세 노트]]

**주요 정리**:
- Handshaking Lemma: $\sum_v \deg(v) = 2|E|$
- 트리: $n$개 노드 → $n-1$개 엣지 (연결 + acyclic)
- 희소 그래프에는 인접 리스트 ($O(n + m)$ 메모리)

---

## 2. 그래프 알고리즘

### 핵심 개념
- **탐색**: BFS (너비 우선), DFS (깊이 우선)
- **최단 경로**: Dijkstra, Bellman-Ford, Floyd-Warshall
- **MST**: Kruskal, Prim
- **중앙성**: Degree, Closeness, Betweenness, PageRank

### 왜 중요한가?
- BFS/DFS = 그래프 분석의 가장 기본 도구
- 최단 경로 = 네비게이션, 라우팅 핵심
- PageRank = 구글 검색의 이론적 기반

### 상세 학습
👉 [[그래프 알고리즘|그래프 알고리즘 상세 노트]]

**주요 정리**:
- BFS는 무가중 그래프에서 최단 경로 보장
- Dijkstra: $O((|V| + |E|) \log |V|)$ (음수 가중치 불가)
- Bellman-Ford: $O(|V| \cdot |E|)$ (음수 가중치 허용)

---

## 3. 스펙트럴 그래프 이론과 GNN

### 핵심 개념
- **Laplacian**: $L = D - A$, quadratic form $x^\top L x = \sum_{(i,j)} (x_i - x_j)^2$
- **고유값**: 0의 multiplicity = 연결 요소 수, $\lambda_2$ = algebraic connectivity
- **스펙트럴 클러스터링**: Laplacian 고유벡터로 임베딩 → K-means
- **GNN**: 메시지 패싱, GCN, GraphSAGE, GAT

### 왜 중요한가?
- Laplacian 고유값이 그래프 전역 구조 함축
- 스펙트럴 클러스터링 = 커뮤니티 탐지 표준 방법
- GNN = 그래프 데이터로 딥러닝의 핵심 기술

### 상세 학습
👉 [[스펙트럴 그래프 이론과 GNN|스펙트럴 이론과 GNN 상세 노트]]

**주요 정리**:
- $L$은 positive semidefinite, $L \mathbf{1} = 0$
- Cheeger's inequality: $\lambda_2$와 graph cut의 관계
- GCN = 1차 spectral filter 근사

---

## 주제별 연결

### 개념 간 관계도

```
그래프 정의 ──────> 노드 + 엣지
    │                     │
    ▼                     ▼
표현 방식 ────> 인접 행렬 / 리스트 / 엣지 리스트
    │                     │
    ▼                     ▼
탐색 알고리즘 ───> BFS ───> 최단 경로 (무가중)
    │             DFS ───> 순환 탐지, 위상 정렬
    ▼
최단 경로 ────> Dijkstra ───> 양의 가중치
              Bellman-Ford ───> 음수 가중치
              Floyd-Warshall ───> 모든 쌍

인접 행렬 ────> Laplacian (L = D - A)
    │                     │
    ▼                     ▼
고유값 분해 ───> 스펙트럴 클러스터링
    │                     │
    ▼                     ▼
Graph Fourier ───> Spectral Filtering ───> GCN
                          │
            ┌─────────────┼─────────────┐
            ▼             ▼             ▼
          GNN         GraphSAGE       GAT
                          │
                          ▼
                  메시지 패싱 프레임워크
```

### 그래프 표현 비교

| 표현 | 메모리 | 엣지 확인 | 이웃 찾기 | 용도 |
|------|--------|----------|----------|------|
| **인접 행렬** | $O(n^2)$ | $O(1)$ | $O(n)$ | 밀집, 행렬 연산 |
| **인접 리스트** | $O(n+m)$ | $O(\deg)$ | $O(\deg)$ | 희소, 탐색 |
| **엣지 리스트** | $O(m)$ | $O(m)$ | $O(m)$ | 데이터 저장, GNN |

---

## 빠른 참조

### 자주 쓰는 공식

**기본 성질**:
- Handshaking: $\sum \deg(v) = 2|E|$
- 완전 그래프: $|E| = \binom{n}{2} = n(n-1)/2$
- 트리: $|E| = |V| - 1$

**최단 경로**:
- BFS: $O(|V| + |E|)$
- Dijkstra: $O((|V| + |E|) \log |V|)$
- Bellman-Ford: $O(|V| \cdot |E|)$
- Floyd-Warshall: $O(|V|^3)$

**Laplacian**:
- $L = D - A$
- $x^\top L x = \sum_{(i,j) \in E} (x_i - x_j)^2$
- Normalized: $L_{sym} = D^{-1/2} L D^{-1/2}$

**GNN**:
- GCN: $H^{(k+1)} = \sigma(\tilde{D}^{-1/2} \tilde{A} \tilde{D}^{-1/2} H^{(k)} W^{(k)})$
- Message Passing: $h_v \leftarrow \text{UPDATE}(h_v, \text{AGG}(\{h_u : u \in \mathcal{N}(v)\}))$

---

## 실습 가이드

### NetworkX 주요 함수

```python
import networkx as nx

# 그래프 생성
G = nx.Graph()  # 무방향
G.add_edge(1, 2, weight=0.5)

# 기본 속성
print(G.number_of_nodes(), G.number_of_edges())
print(dict(G.degree()))

# 최단 경로
path = nx.shortest_path(G, source=1, target=5)
length = nx.shortest_path_length(G, 1, 5, weight='weight')

# 중앙성
degree_cent = nx.degree_centrality(G)
pagerank = nx.pagerank(G)

# Laplacian
L = nx.laplacian_matrix(G).todense()

# 클러스터링
clustering = nx.clustering(G)
```

### PyTorch Geometric

```python
import torch
from torch_geometric.data import Data
from torch_geometric.nn import GCNConv

# 그래프 생성
edge_index = torch.tensor([[0, 1, 1, 2], [1, 0, 2, 1]], dtype=torch.long)
x = torch.randn(3, 16)  # 3 nodes, 16 features
data = Data(x=x, edge_index=edge_index)

# GCN 레이어
conv = GCNConv(16, 32)
out = conv(data.x, data.edge_index)
```

---

## 딥러닝 연결 포인트

### 어디에 쓰이나?

| 응용 분야 | 그래프 이론 | 관련 노트 |
|----------|-----------|-----------|
| **소셜 네트워크 분석** | 중앙성, 커뮤니티 탐지 | [[그래프 알고리즘]] |
| **추천 시스템** | 이분 그래프, 임베딩 | [[스펙트럴 그래프 이론과 GNN]] |
| **분자 특성 예측** | GNN | [[스펙트럴 그래프 이론과 GNN]] |
| **지식 그래프** | 그래프 임베딩 | [[스펙트럴 그래프 이론과 GNN]] |
| **교통 네트워크** | 최단 경로 | [[그래프 알고리즘]] |
| **이미지 분할** | Graph cut, spectral clustering | [[스펙트럴 그래프 이론과 GNN]] |

---

## 참고 자료

### 교과서
- Reinhard Diestel, *Graph Theory*, 5th ed.
- Fan R. K. Chung, *Spectral Graph Theory*
- Mark Newman, *Networks: An Introduction*

### 논문
- Kipf & Welling (2017), "Semi-Supervised Classification with Graph Convolutional Networks"
- Hamilton et al. (2017), "Inductive Representation Learning on Large Graphs"
- Veličković et al. (2018), "Graph Attention Networks"

### 온라인 강의
- Stanford CS224W: Machine Learning with Graphs
- MIT 6.042J: Graph Theory
- Fast.ai: Deep Learning for Coders (Graph section)

### 라이브러리
- NetworkX: https://networkx.org/
- PyTorch Geometric: https://pytorch-geometric.readthedocs.io/
- DGL: https://www.dgl.ai/

---

## 다음 학습

그래프 이론을 마쳤다면:

1. **선형대수와 융합**
   - [[Math/Linear Algebra/고유값과 고유벡터|고유값]]
   - [[Math/Linear Algebra/행렬과 선형 변환|행렬]]
   - Laplacian 고유값 분석

2. **최적화 연결**
   - [[Math/Optimization/최적화 기초|최적화 기초]]
   - Graph cut, network flow

3. **실전 응용**
   - [[ML Foundations/학습 패러다임|학습 패러다임]]
   - Semi-supervised learning with graphs

4. **상위 개념**
   - [[AI 기초 개요|AI 기초 전체 로드맵]]
