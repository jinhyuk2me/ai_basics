## TL;DR
- 벡터 공간(Vector Space)은 벡터 덧셈과 스칼라 곱이 정의되고 8가지 공리를 만족하는 대수 구조로, 선형대수의 가장 기본적인 개념이다.
- 기저(Basis)는 벡터 공간을 생성하면서 동시에 선형 독립인 벡터들의 집합이며, 차원(Dimension)은 기저 벡터의 개수로 정의된다.
- 딥러닝에서 특징 벡터, 임베딩, 파라미터 공간 모두 벡터 공간의 개념으로 이해할 수 있으며, 기저 변환은 좌표계 변경(예: RGB ↔ YUV)과 직결된다.

---

## 1. 벡터 공간의 정의

### 1.1 공리적 정의

**정의 (벡터 공간)**: 집합 $V$와 체(field) $\mathbb{F}$ (보통 $\mathbb{R}$ 또는 $\mathbb{C}$)에 대해, 다음 두 연산이 정의되어 있을 때 $V$를 $\mathbb{F}$ 위의 벡터 공간이라 한다:

```
💡 벡터 공간의 쉬운 정의
벡터 공간(Vector Space)이란,  
“벡터끼리 더하고, 스칼라(숫자)로 곱했을 때도 여전히 같은 종류의 벡터가 되는 공간”이에요.

💡 구성 요소
- 집합 V : 벡터들이 모여 있는 공간 (예: 모든 2차원 벡터의 집합 등)
- 체(field) F : 벡터에 곱할 수 있는 스칼라의 집합 (보통 실수 R 또는 복소수 C)
```

1. 벡터 덧셈 
$$+: V \times V \rightarrow V$$
2. 스칼라 곱셈
$$\cdot: \mathbb{F} \times V \rightarrow V$$

```
직관적으로 보면
- 벡터 공간은 “덧셈과 스칼라 곱이 가능한 집합”이에요.
- 벡터의 모양(화살표, 배열 등)은 중요하지 않고, 이 연산들이 어떤 규칙을 따르는지가 중요합니다.
  
벡터 공간의 정의는
“무엇이 벡터인가?” 보다는
“벡터들 사이의 연산이 어떤 성질을 가져야 하는가?” 
를 공리(axiom)로 정해놓은 거예요.
``` 

이 연산들은 다음 8가지 공리를 만족해야 한다:

**덧셈에 관한 공리**:
1. **결합법칙**: $(\mathbf{u} + \mathbf{v}) + \mathbf{w} = \mathbf{u} + (\mathbf{v} + \mathbf{w})$
2. **교환법칙**: $\mathbf{u} + \mathbf{v} = \mathbf{v} + \mathbf{u}$
3. **항등원 존재**: $\mathbf{v} + \mathbf{0} = \mathbf{v}$를 만족하는 $\mathbf{0} \in V$ 존재
4. **역원 존재**: 모든 $\mathbf{v}$에 대해 $\mathbf{v} + (-\mathbf{v}) = \mathbf{0}$인 $-\mathbf{v} \in V$ 존재

**스칼라 곱에 관한 공리**:
5. **스칼라 곱의 결합법칙**: $a(b\mathbf{v}) = (ab)\mathbf{v}$
6. **항등원**: $1 \cdot \mathbf{v} = \mathbf{v}$
7. **분배법칙 I**: $a(\mathbf{u} + \mathbf{v}) = a\mathbf{u} + a\mathbf{v}$
8. **분배법칙 II**: $(a + b)\mathbf{v} = a\mathbf{v} + b\mathbf{v}$

### 1.2 벡터 공간의 예시

**예제 1**: $\mathbb{R}^n = \{(x_1, x_2, \ldots, x_n) : x_i \in \mathbb{R}\}$
- 일반적인 벡터 덧셈과 스칼라 곱으로 벡터 공간을 이룬다.
- 딥러닝에서 가장 자주 등장하는 공간. 가중치 벡터, 특징 벡터 모두 $\mathbb{R}^n$의 원소.

**예제 2**: 다항식 공간 $P_n = \{a_0 + a_1 x + \cdots + a_n x^n : a_i \in \mathbb{R}\}$
- 차수가 $n$ 이하인 모든 다항식의 집합.
- 함수 근사(polynomial regression)에서 사용.

**예제 3**: 행렬 공간 $\mathbb{R}^{m \times n}$
- 모든 $m \times n$ 실수 행렬의 집합.
- 신경망의 가중치 행렬이 이 공간의 원소.

**예제 4**: 연속 함수 공간 $C[a, b]$
- 구간 $[a, b]$에서 연속인 모든 함수의 집합.
- 함수형 데이터 분석, 신경망의 함수 공간 해석에서 등장.

### 1.3 부분공간 (Subspace)

**정의 (부분공간)**: 벡터 공간 $V$의 부분집합 $W$가 다음을 만족하면 $W$를 $V$의 부분공간이라 한다:
1. $\mathbf{0} \in W$ (영벡터 포함)
2. $\mathbf{u}, \mathbf{v} \in W \Rightarrow \mathbf{u} + \mathbf{v} \in W$ (덧셈에 닫혀 있음)
3. $\mathbf{v} \in W, c \in \mathbb{F} \Rightarrow c\mathbf{v} \in W$ (스칼라 곱에 닫혀 있음)

**예제**: $\mathbb{R}^3$에서 원점을 지나는 평면이나 직선은 모두 부분공간이다.

**비예제**: 원점을 지나지 않는 평면은 부분공간이 아니다 (영벡터를 포함하지 않음).

---

## 2. 벡터의 표현

### 2.1 열벡터와 행벡터

**열벡터 (Column Vector)**:
$$
\mathbf{v} = \begin{bmatrix} v_1 \\ v_2 \\ v_3 \end{bmatrix}
$$

- 세로로 늘어선 형태
- 공간의 한 점이나 방향(좌표)을 나타냄
- 선형대수에서 "기본적인 벡터"는 대부분 이 형태로 생각함

예를 들어 $\mathbf{v} = \begin{bmatrix} 2 \\ 1 \end{bmatrix}$이면, "x축으로 2, y축으로 1 이동한 점" 또는 "(2,1) 방향의 벡터"를 의미한다.

**행벡터 (Row Vector)**:
$$
\mathbf{v}^T = [v_1, v_2, v_3]
$$

- 가로로 나열된 형태
- 벡터를 이용해 다른 벡터에 작용하는(곱하는) 관점
- "함수처럼" 생각함

예를 들어 $[2, 1]$은 "입력 벡터의 x성분에 2, y성분에 1을 곱해 더한다"는 연산을 뜻할 수도 있다.

### 2.2 행렬 안에서의 의미

행렬 $A \in \mathbb{R}^{m \times n}$을 보면, 그 안에 두 가지 관점의 벡터 묶음이 들어 있다:

| 관점 | 구성 요소 | 의미 |
|------|----------|------|
| 열벡터 (column vectors) | 각 열(column) | 입력 공간의 축 벡터가 변환된 결과 |
| 행벡터 (row vectors) | 각 행(row) | 출력 공간에서 각 좌표 성분이 입력에 어떻게 의존하는가 |

**예시**:
$$
A = \begin{bmatrix} 1 & 2 \\ 3 & 4 \end{bmatrix}
$$

**열벡터 관점**:
$$
\mathbf{v}_1 = \begin{bmatrix} 1 \\ 3 \end{bmatrix}, \quad \mathbf{v}_2 = \begin{bmatrix} 2 \\ 4 \end{bmatrix}
$$

이 두 벡터는 단위 벡터 $\mathbf{e}_1 = (1, 0)$, $\mathbf{e}_2 = (0, 1)$가 $A$에 의해 어디로 가는지를 보여준다. 즉, $A$가 공간을 어떻게 변형시키는지를 나타낸다.

**행벡터 관점**:
$$
\mathbf{r}_1 = [1, 2], \quad \mathbf{r}_2 = [3, 4]
$$

출력의 각 성분이 입력의 어떤 조합으로 만들어지는지를 보여준다. 즉, 각 좌표 성분이 입력 벡터와 어떤 관계를 가지는가를 나타낸다.

### 2.3 행과 열의 관계 요약

| 구분 | 열벡터 | 행벡터 |
|------|--------|--------|
| 모양 | 세로형 | 가로형 |
| 표기 | $\mathbf{v}$ | $\mathbf{v}^T$ |
| 의미 | 공간상의 점/방향 | 선형함수(입력에 작용) |
| 행렬에서 역할 | 입력 축이 변환된 결과 | 출력 성분 계산 방식 |
| 전치 시 역할 | 서로 바뀜 | 서로 바뀜 |

### 2.4 전치(Transpose)와의 연결

전치는 바로 이 "행 ↔ 열"의 입장 전환이다.

$$
A = \begin{bmatrix} 1 & 2 \\ 3 & 4 \end{bmatrix}
\Rightarrow
A^T = \begin{bmatrix} 1 & 3 \\ 2 & 4 \end{bmatrix}
$$

원래는 열벡터들이 $(1, 3)$, $(2, 4)$였다면, 전치하면 그것이 행벡터로 바뀐다.

**핵심**: 전치는 "기하학적 변환 자체를 바꾸는 것이 아니라, 그 변환을 바라보는 관점을 바꾸는 것"이다.

---

## 3. 벡터 연산

### 3.1 내적 (Dot Product)

**정의**: 두 벡터 $\mathbf{a}, \mathbf{b}$의 내적은
$$
\mathbf{a} \cdot \mathbf{b} = |\mathbf{a}|\,|\mathbf{b}| \cos\theta
$$

여기서
- $|\mathbf{a}|, |\mathbf{b}|$: 각 벡터의 크기
- $\theta$: 두 벡터 사이의 각도

**성분 표현**: $\mathbf{a} = (a_1, a_2, \ldots, a_n)$, $\mathbf{b} = (b_1, b_2, \ldots, b_n)$일 때
$$
\mathbf{a} \cdot \mathbf{b} = a_1 b_1 + a_2 b_2 + \cdots + a_n b_n
$$

**기하학적 의미**:
- 한 벡터가 다른 벡터 방향으로 얼마나 투영(projection)되는지를 나타냄
- "두 벡터의 방향이 얼마나 비슷한가"를 측정

$$
\mathbf{a} \cdot \mathbf{b} = |\mathbf{a}|\,|\mathbf{b}| \cos\theta \Rightarrow
\begin{cases}
> 0 & \text{(같은 방향)} \\
= 0 & \text{(직교)} \\
< 0 & \text{(반대 방향)}
\end{cases}
$$

**투영(Projection)**:
$\mathbf{b}$가 $\mathbf{a}$ 방향으로 얼마나 들어가 있는지를 보면, 그 "그림자 길이"가 내적의 핵심이다.
$$
\text{proj}_{\mathbf{a}}(\mathbf{b}) = \frac{\mathbf{a} \cdot \mathbf{b}}{|\mathbf{a}|}
$$

**예시**: $\mathbf{a} = (1, 0)$, $\mathbf{b} = (\cos\theta, \sin\theta)$
$$
\mathbf{a} \cdot \mathbf{b} = 1 \cdot \cos\theta + 0 \cdot \sin\theta = \cos\theta
$$
→ $\cos\theta$가 곧 두 벡터의 방향 유사도

### 3.2 외적 (Cross Product)

**정의 (3D에서만 정의됨)**:
$$
\mathbf{a} \times \mathbf{b} =
\begin{bmatrix}
a_y b_z - a_z b_y \\
a_z b_x - a_x b_z \\
a_x b_y - a_y b_x
\end{bmatrix}
$$

**기하학적 의미**:
- 외적의 크기: $|\mathbf{a} \times \mathbf{b}| = |\mathbf{a}|\,|\mathbf{b}| \sin\theta$
  - 이는 두 벡터가 만드는 평행사변형의 면적
- 방향: 외적의 결과는 두 벡터에 수직인 벡터
  - 오른손 법칙(Right-hand rule)을 따름
  - 오른손을 $\mathbf{a}$ → $\mathbf{b}$ 방향으로 돌릴 때, 엄지손가락이 향하는 방향이 $\mathbf{a} \times \mathbf{b}$

### 3.3 내적과 외적 비교

| 항목 | 내적 (Dot Product) | 외적 (Cross Product) |
|------|-------------------|---------------------|
| 결과 | 스칼라 (scalar) | 벡터 (vector) |
| 식 | $\mathbf{a} \cdot \mathbf{b} = \|\mathbf{a}\|\,\|\mathbf{b}\| \cos\theta$ | $\|\mathbf{a} \times \mathbf{b}\| = \|\mathbf{a}\|\,\|\mathbf{b}\| \sin\theta$ |
| 기하학적 의미 | 방향 유사도, 투영 | 평행사변형 면적, 수직 방향 |
| 0이 되는 경우 | 직각일 때 | 평행일 때 |
| 차원 | 모든 차원에서 정의 | 3차원에서만 정의 |

### 3.4 2D에서 외적의 변형 (의사 외적)

2D에서는 결과가 벡터가 아니라 스칼라 값으로 나온다:
$$
\mathbf{a} = (a_x, a_y), \quad \mathbf{b} = (b_x, b_y) \Rightarrow \mathbf{a} \times \mathbf{b} = a_x b_y - a_y b_x
$$

이는 행렬식과 동일하며, 절댓값이 두 벡터가 만드는 평행사변형의 면적이다.

**정리**: "내적은 방향의 유사도(투영)를, 외적은 면적과 수직 방향을 나타낸다."

---

## 4. 선형 결합과 Span

### 4.1 선형 결합

**정의 (선형 결합)**: 벡터 $\mathbf{v}_1, \mathbf{v}_2, \ldots, \mathbf{v}_k \in V$와 스칼라 $c_1, c_2, \ldots, c_k \in \mathbb{F}$에 대해
$$
c_1 \mathbf{v}_1 + c_2 \mathbf{v}_2 + \cdots + c_k \mathbf{v}_k
$$
를 $\mathbf{v}_1, \ldots, \mathbf{v}_k$의 선형 결합(linear combination)이라 한다.

### 4.2 Span

**정의 (Span)**: 벡터들 $\{\mathbf{v}_1, \ldots, \mathbf{v}_k\}$의 모든 선형 결합으로 이루어진 집합을 span이라 하며, 다음과 같이 표기한다:
$$
\operatorname{span}\{\mathbf{v}_1, \ldots, \mathbf{v}_k\} = \left\{ \sum_{i=1}^k c_i \mathbf{v}_i : c_i \in \mathbb{F} \right\}
$$

**정리**: $\operatorname{span}\{\mathbf{v}_1, \ldots, \mathbf{v}_k\}$는 $V$의 부분공간이다.

**증명**:
1. $\mathbf{0} = 0\mathbf{v}_1 + \cdots + 0\mathbf{v}_k \in \operatorname{span}\{\mathbf{v}_1, \ldots, \mathbf{v}_k\}$
2. $\mathbf{u} = \sum a_i \mathbf{v}_i, \mathbf{w} = \sum b_i \mathbf{v}_i$이면
   $$\mathbf{u} + \mathbf{w} = \sum (a_i + b_i) \mathbf{v}_i \in \operatorname{span}$$
3. $c\mathbf{u} = \sum (ca_i) \mathbf{v}_i \in \operatorname{span}$

**예제**: $\mathbb{R}^3$에서 $\mathbf{v}_1 = (1, 0, 0)$, $\mathbf{v}_2 = (0, 1, 0)$이면
$$\operatorname{span}\{\mathbf{v}_1, \mathbf{v}_2\} = \{(x, y, 0) : x, y \in \mathbb{R}\}$$
이는 xy-평면이다.

---

## 5. 선형 독립과 선형 종속

### 5.1 정의

**정의 (선형 독립)**: 벡터 $\mathbf{v}_1, \ldots, \mathbf{v}_k$가 선형 독립(linearly independent)이다 $\Leftrightarrow$
$$
c_1 \mathbf{v}_1 + \cdots + c_k \mathbf{v}_k = \mathbf{0} \Rightarrow c_1 = \cdots = c_k = 0
$$

즉, 영벡터를 만드는 유일한 방법은 모든 계수를 0으로 하는 것뿐이다.

**정의 (선형 종속)**: 선형 독립이 아니면 선형 종속(linearly dependent)이다. 즉, 어떤 영이 아닌 계수로 영벡터를 만들 수 있다.

### 5.2 기하학적 직관

**2차원**:
- 두 벡터가 평행하면 선형 종속 (하나가 다른 하나의 스칼라배)
- 두 벡터가 평행하지 않으면 선형 독립

**3차원**:
- 세 벡터가 같은 평면 위에 있으면 선형 종속
- 같은 평면에 있지 않으면 선형 독립

### 5.3 선형 독립 판별법

**정리**: 벡터 $\mathbf{v}_1, \ldots, \mathbf{v}_k \in \mathbb{R}^n$을 열로 하는 행렬을 $A = [\mathbf{v}_1 \cdots \mathbf{v}_k]$라 하면,
- 이들이 선형 독립 $\Leftrightarrow$ $A\mathbf{x} = \mathbf{0}$의 해가 $\mathbf{x} = \mathbf{0}$뿐
- 이들이 선형 독립 $\Leftrightarrow$ $\operatorname{rank}(A) = k$
- $k = n$이고 정사각 행렬이면 $\Leftrightarrow$ $\det(A) \neq 0$

**예제**: $\mathbf{v}_1 = (1, 2, 3)$, $\mathbf{v}_2 = (2, 4, 6)$, $\mathbf{v}_3 = (1, 0, 1)$
- $\mathbf{v}_2 = 2\mathbf{v}_1$이므로 선형 종속

---

## 6. 기저와 차원

### 6.1 기저의 정의

**정의 (기저)**: 벡터 공간 $V$의 부분집합 $\mathcal{B} = \{\mathbf{b}_1, \ldots, \mathbf{b}_n\}$가 다음을 만족하면 $V$의 기저(basis)라 한다:
1. $\operatorname{span}(\mathcal{B}) = V$ (생성 조건)
2. $\mathcal{B}$의 벡터들이 선형 독립 (독립 조건)

**핵심**: 기저는 공간을 "최소한의 벡터로 표현"하는 좌표계이다.

### 6.2 표준 기저

**$\mathbb{R}^n$의 표준 기저**:
$$
\mathbf{e}_1 = (1, 0, \ldots, 0), \quad \mathbf{e}_2 = (0, 1, 0, \ldots, 0), \quad \ldots, \quad \mathbf{e}_n = (0, \ldots, 0, 1)
$$

**다항식 공간 $P_n$의 표준 기저**:
- 다항식 공간 $P_n$ 은 "최대 차수가 $n$"인 모든 다항식들의 집합이야.

$$
P_2 = \{ a_0 + a_1x + a_2x^2 \mid a_i \in \mathbb{R} \}
$$
- 이때 표준 기저 -> 왜냐면 모든 다항식은 이 기저들의 선형결합으로 표현되니까.
$$
\{ 1, x, x^2, \dots, x^n \}
$$
- 예를 들어,
$$
3 + 2x + 5x^2 = 3(1) + 2(x) + 5(x^2)
$$
```
표준 기저는 벡터공간을 구성하는 가장 기본적인 축(direction) 세트다.
```
### 6.3 차원

**정리 (차원의 유일성)**: 벡터 공간 $V$의 모든 기저는 같은 개수의 벡터를 갖는다.
```
즉, 기저가 여러 가지여도, 그 안에 포함된 벡터의 개수(= 차원)는 항상 일정하다.
```

**증명 스케치**: 두 기저 $\{\mathbf{b}_1, \ldots, \mathbf{b}_n\}$, $\{\mathbf{c}_1, \ldots, \mathbf{c}_m\}$가 있다고 하자.
- $\mathbf{c}_1$은 $\mathbf{b}_i$들의 선형 결합으로 표현 가능
- [[6. 슈타이니츠 교환 정리]]에 의해 $m \leq n$
- 역으로 $n \leq m$도 성립
- 따라서 $m = n$

**정의 (차원)**: 벡터 공간 $V$의 기저에 포함된 벡터의 개수를 $V$의 차원(dimension)이라 하고 $\dim(V)$로 표기한다.

**예제**:
- $\dim(\mathbb{R}^n) = n$
- $\dim(P_n) = n + 1$ (기저는 $\{1, x, \ldots, x^n\}$)
- $\dim(\mathbb{R}^{m \times n}) = mn$

### 6.4 좌표 표현

**정리**: 벡터 공간 $V$의 기저 $\mathcal{B} = \{\mathbf{b}_1, \ldots, \mathbf{b}_n\}$가 주어졌을 때, 모든 $\mathbf{v} \in V$는 **유일하게**
$$
\mathbf{v} = c_1 \mathbf{b}_1 + \cdots + c_n \mathbf{b}_n
$$
로 표현된다. 이때 $(c_1, \ldots, c_n)$을 기저 $\mathcal{B}$에 대한 $\mathbf{v}$의 좌표(coordinate)라 한다.

**증명**:
- 존재성: 기저는 $V$를 생성하므로 표현 가능
- 유일성: 두 표현 $\mathbf{v} = \sum c_i \mathbf{b}_i = \sum d_i \mathbf{b}_i$가 있으면
  $$\sum (c_i - d_i) \mathbf{b}_i = \mathbf{0}$$
  선형 독립성에 의해 $c_i - d_i = 0$ for all $i$

---

## 7. 기저 변환

### 7.1 변환 행렬

두 기저 $\mathcal{B} = \{\mathbf{b}_1, \ldots, \mathbf{b}_n\}$, $\mathcal{C} = \{\mathbf{c}_1, \ldots, \mathbf{c}_n\}$가 주어졌을 때,

각 $\mathbf{c}_j$를 $\mathcal{B}$ 기저로 표현하면:
$$
\mathbf{c}_j = \sum_{i=1}^n p_{ij} \mathbf{b}_i
$$

**변환 행렬** $P = [p_{ij}]$는 기저 $\mathcal{C}$에 대한 좌표를 기저 $\mathcal{B}$에 대한 좌표로 변환한다:
$$
[\mathbf{v}]_{\mathcal{B}} = P [\mathbf{v}]_{\mathcal{C}}
$$

### 7.2 예제: RGB ↔ YUV 변환

RGB 색 공간에서 YUV 색 공간으로의 변환은 기저 변환의 대표적 예시이다:
$$
\begin{bmatrix} Y \\ U \\ V \end{bmatrix}
=
\begin{bmatrix}
0.299 & 0.587 & 0.114 \\
-0.147 & -0.289 & 0.436 \\
0.615 & -0.515 & -0.100
\end{bmatrix}
\begin{bmatrix} R \\ G \\ B \end{bmatrix}
$$

---

## 8. 랭크와 Nullity

### 8.1 행렬의 랭크

#### 개념 직관
- 열 랭크: 열벡터들이 뻗는 공간(열공간)의 차원 -> 서로 "진짜로 다른 방향"이 몇 개인가
- 행 랭크: 행벡터들이 뻗는 공간(행공간)의 차원

#### 정의
$$
\text{colspace}(A)=\operatorname{span}\{\text{columns of }A\},\quad
\text{rowspace}(A)=\operatorname{span}\{\text{rows of }A\}

$$
$$
\operatorname{rank}(A)=\dim(\text{colspace}(A))=\dim(\text{rowspace}(A))

$$
#### 정리 (행 랭크 = 열 랭크)
$$
\dim(\text{rowspace}(A))=\dim(\text{colspace}(A))

$$

### 8.2 Null space와 Nullity
#### 정의
$$
\ker(A)=\{x\in\mathbb{R}^n\mid A x=0\},\qquad
\operatorname{nullity}(A)=\dim(\ker(A))

$$
- $ker(A)$는 $A$가 **완전히 죽여버리는 방향들**의 집합
- 자유변수의 개수 = $\operatorname{nullity}(A)$

### 8.3 Rank-Nullity 정리

$$
A\in\mathbb{R}^{m\times n}\ \Rightarrow\ 
\operatorname{rank}(A)+\operatorname{nullity}(A)=n
$$
**증명 스케치(가우스 소거 관점)**
- RREF에서 **피벗 변수 개수** = rank(A)
- **자유 변수 개수** = nullity(A) 
- 전체 변수 수 n=n=n= 피벗 + 자유 ⇒ 공식이 바로 성립.

---

## 9. PyTorch 실습

```python
import torch
import numpy as np

# 1. 선형 독립성 확인
v1 = torch.tensor([1., 2., 3.])
v2 = torch.tensor([2., 4., 6.])
v3 = torch.tensor([1., 0., 1.])

A = torch.stack([v1, v2, v3], dim=1)  # 열로 배치
rank = torch.linalg.matrix_rank(A)
print(f"Rank: {rank.item()}")  # 2 (v1, v2가 종속이므로)

# 2. 기저 변환 예제
# 표준 기저에서 회전된 기저로 변환
theta = torch.tensor(np.pi / 4)  # 45도 회전
P = torch.tensor([
    [torch.cos(theta), -torch.sin(theta)],
    [torch.sin(theta),  torch.cos(theta)]
])

v = torch.tensor([1., 0.])  # 표준 기저에서의 좌표
v_rotated = P @ v  # 회전된 기저에서의 좌표
print(f"Original: {v}")
print(f"Rotated: {v_rotated}")

# 3. Null space 차원 확인
A = torch.tensor([
    [1., 2., 3.],
    [2., 4., 6.]
], dtype=torch.float64)

rank = torch.linalg.matrix_rank(A)
n = A.shape[1]
nullity = n - rank
print(f"Rank: {rank.item()}, Nullity: {nullity.item()}")
# Rank-Nullity 정리: 1 + 2 = 3
```

---

## 10. 연습 문제

1. **공리 확인**
   집합 $\mathbb{R}^2$에서 덧셈을 $(x_1, y_1) \oplus (x_2, y_2) = (x_1 + x_2 + 1, y_1 + y_2)$로 정의하면 벡터 공간이 되는가? 어떤 공리가 위배되는지 확인하라.

2. **부분공간 판별**
   다음 중 $\mathbb{R}^3$의 부분공간인 것을 모두 고르고, 부분공간이 아닌 경우 반례를 제시하라:
   - $W_1 = \{(x, y, 0) : x, y \in \mathbb{R}\}$
   - $W_2 = \{(x, y, z) : x + y + z = 1\}$
   - $W_3 = \{(x, 2x, 3x) : x \in \mathbb{R}\}$

3. **선형 독립 판별**
   벡터 $(1, 2, 1)$, $(2, 1, 3)$, $(3, 3, 4)$가 선형 독립인지 판별하고, 종속이면 선형 관계를 구하라.

4. **기저 찾기**
   부분공간 $W = \{(x, y, z, w) : x + y = 0, z - w = 0\}$의 기저와 차원을 구하라.

5. **기저 변환**
   $\mathbb{R}^2$에서 표준 기저 $\mathcal{E} = \{(1,0), (0,1)\}$와 기저 $\mathcal{B} = \{(1,1), (1,-1)\}$에 대해, 벡터 $(3, 1)$의 $\mathcal{B}$-좌표를 구하라.

6. **Rank-Nullity**
   $3 \times 5$ 행렬 $A$의 랭크가 2일 때, $A\mathbf{x} = \mathbf{0}$의 해공간 차원은?

---

## 11. 참고 자료

- Gilbert Strang, *Introduction to Linear Algebra*, 5th ed., Chapter 2-3
- David C. Lay, *Linear Algebra and Its Applications*, Chapter 1-4
- Sheldon Axler, *Linear Algebra Done Right*, Chapter 1-2
- MIT OCW 18.06, Lectures 1-10
- 3Blue1Brown, *Essence of Linear Algebra*, Episode 1-7

---

## 12. 다음 학습

- [[Math/Linear Algebra/3. 행렬과 선형 변환|행렬과 선형 변환]]
- [[Math/Linear Algebra/4. 고유값과 고유벡터|고유값과 고유벡터]]
- [[Math/Linear Algebra/1. 선형대수 핵심 개요|선형대수 전체 개요]]
- [[Math/Reference/개념 용어집|수학 용어집]]
