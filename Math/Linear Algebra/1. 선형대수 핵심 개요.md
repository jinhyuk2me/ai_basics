## TL;DR
- 선형대수는 **벡터 공간과 선형 변환**을 다루는 학문으로, 신경망의 가중치 행렬, 어텐션 점수 계산, 차원 축소(PCA), SVD 기반 압축 등 거의 모든 딥러닝 기법의 수학적 토대를 제공한다.
- 이 노트는 선형대수 전체 내용의 허브로, 각 주제별 상세 노트로 연결하는 로드맵 역할을 한다.
- 벡터 공간 → 행렬 연산 → 고유값/고유벡터 → SVD 순서로 학습하면 체계적으로 이해할 수 있다.

---

## 학습 로드맵

| 순서 | 주제 | 핵심 질문 | 바로 가기 |
|------|------|-----------|-----------|
| 1 | 벡터 공간과 기저 | 벡터 공간이란 무엇이며, 기저는 왜 중요한가? | [[벡터 공간과 기저]] |
| 2 | 행렬과 선형 변환 | 행렬 곱셈의 기하학적 의미는? 역행렬은 언제 존재하는가? | [[행렬과 선형 변환]] |
| 3 | 고유값과 고유벡터 | 변환 후에도 방향이 유지되는 벡터는? 대각화는 왜 유용한가? | [[고유값과 고유벡터]] |
| 4 | 특이값 분해 | 모든 행렬을 어떻게 분해하는가? SVD의 응용은? | [[특잇값 분해]] |

---

## 1. 벡터 공간과 기저

### 핵심 개념
- **벡터 공간**: 벡터 덧셈과 스칼라 곱이 8가지 공리를 만족하는 대수 구조
- **선형 결합**: 벡터들의 가중합 $c_1\mathbf{v}_1 + \cdots + c_k\mathbf{v}_k$
- **선형 독립**: 어떤 벡터도 다른 벡터들의 선형 결합으로 표현 불가
- **기저**: 공간을 생성하면서 선형 독립인 벡터들의 집합
- **차원**: 기저 벡터의 개수

### 왜 중요한가?
- 딥러닝의 특징 벡터, 임베딩, 파라미터 공간 모두 벡터 공간
- 기저 변환은 좌표계 변경 (RGB ↔ YUV, PCA 변환 등)
- 차원 개념은 모델 복잡도와 직결

### 상세 학습
👉 [[벡터 공간과 기저|벡터 공간과 기저 상세 노트]]

**주요 정리**:
- 벡터 공간의 모든 기저는 같은 개수의 벡터를 갖는다
- Rank-Nullity 정리: $\operatorname{rank}(A) + \operatorname{nullity}(A) = n$

---

## 2. 행렬과 선형 변환

### 핵심 개념
- **선형 변환**: $T(c_1\mathbf{v}_1 + c_2\mathbf{v}_2) = c_1 T(\mathbf{v}_1) + c_2 T(\mathbf{v}_2)$
- **행렬 표현**: 모든 선형 변환은 행렬로 표현 가능
- **행렬 곱셈**: 변환의 합성
- **역행렬**: 변환을 되돌리는 연산
- **행렬식**: 부피 변환 계수, 가역성 판별

### 왜 중요한가?
- 신경망의 완전연결층 $y = Wx + b$에서 $W$가 선형 변환
- 행렬 곱셈 연산이 딥러닝 계산의 대부분
- 직교 행렬은 BatchNorm, Spectral Normalization에서 활용

### 상세 학습
👉 [[행렬과 선형 변환|행렬과 선형 변환 상세 노트]]

**주요 정리**:
- $\det(AB) = \det(A)\det(B)$
- $A$가 가역 $\Leftrightarrow$ $\det(A) \neq 0$ $\Leftrightarrow$ $\operatorname{rank}(A) = n$
- 직교 행렬: $Q^\top Q = I$, 길이와 각도 보존

---

## 3. 고유값과 고유벡터

### 핵심 개념
- **고유벡터**: $A\mathbf{v} = \lambda\mathbf{v}$ (변환 후 방향 유지)
- **고유값**: 고유벡터의 스케일 배율
- **특성 방정식**: $\det(A - \lambda I) = 0$
- **대각화**: $A = PDP^{-1}$ (단, $P$는 고유벡터 행렬)
- **스펙트럴 정리**: 대칭 행렬은 직교 대각화 가능

### 왜 중요한가?
- PCA는 공분산 행렬의 고유벡터를 주성분으로 사용
- Hessian 고유값으로 극소/극대/안장점 판별
- 그래프 라플라시안의 고유값으로 클러스터링 (Spectral clustering)
- 시스템 안정성 분석 (RNN의 gradient explosion)

### 상세 학습
👉 [[고유값과 고유벡터|고유값과 고유벡터 상세 노트]]

**주요 정리**:
- 대칭 행렬의 고유값은 모두 실수
- $n \times n$ 행렬이 대각화 가능 $\Leftrightarrow$ $n$개의 선형 독립 고유벡터 존재
- 조건수 $\kappa(H) = \lambda_{\max}/\lambda_{\min}$이 크면 최적화 어려움

---

## 4. 특이값 분해 (SVD)

### 핵심 개념
- **SVD**: $A = U\Sigma V^\top$ (모든 행렬에 적용 가능)
- **특이값**: $\Sigma$의 대각 성분 $\sigma_1 \geq \sigma_2 \geq \cdots$
- **Rank-k 근사**: 상위 $k$개 특이값만 사용
- **Pseudo-inverse**: $A^+ = V\Sigma^+ U^\top$
- **Eckart-Young 정리**: $A_k$는 최적 저랭크 근사

### 왜 중요한가?
- PCA의 수학적 토대 (데이터 행렬의 SVD = 주성분)
- 이미지 압축, 노이즈 제거
- 추천 시스템의 matrix factorization (Netflix Prize)
- 조건수 계산 ($\kappa = \sigma_{\max}/\sigma_{\min}$)
- Over/underdetermined 시스템의 최소제곱 해

### 상세 학습
👉 [[특잇값 분해|특이값 분해 상세 노트]]

**주요 정리**:
- $A^\top A$의 고유벡터 = $V$ (우특이벡터)
- $A^\top A$의 고유값 = $\sigma_i^2$
- Rank-k 근사 오차: $\|A - A_k\|_F = \sqrt{\sigma_{k+1}^2 + \cdots + \sigma_r^2}$

---

## 주제별 연결

### 개념 간 관계도

```
벡터 공간 ────> 선형 변환 ────> 행렬 표현
    │                           │
    │                           ▼
    └────────────────> 기저 변환 행렬
                                │
                                ▼
        고유값 분해 ◄──── 대칭 행렬 (A = A^T)
            │                   │
            ▼                   ▼
        대각화            스펙트럴 정리
            │                   │
            └───────┬───────────┘
                    ▼
              특이값 분해 (SVD)
                    │
        ┌───────────┼───────────┐
        ▼           ▼           ▼
       PCA    이미지 압축    추천 시스템
```

### 핵심 분해 비교

| 분해 방법 | 형태 | 조건 | 주요 응용 |
|----------|------|------|-----------|
| **고유값 분해** | $A = PDP^{-1}$ | 정사각, 대각화 가능 | 거듭제곱, 미분방정식 |
| **스펙트럴 분해** | $A = QDQ^\top$ | 대칭 행렬 | PCA, Hessian 분석 |
| **SVD** | $A = U\Sigma V^\top$ | 모든 행렬 | 차원 축소, 압축, pseudo-inverse |
| **LU 분해** | $A = LU$ | 정사각 (피벗 필요) | 선형 시스템 |
| **QR 분해** | $A = QR$ | 모든 행렬 | 최소제곱, Gram-Schmidt |

---

## 빠른 참조

### 자주 쓰는 공식

**행렬 연산**:
- $(AB)^\top = B^\top A^\top$
- $(AB)^{-1} = B^{-1}A^{-1}$
- $\det(AB) = \det(A)\det(B)$
- $\operatorname{trace}(AB) = \operatorname{trace}(BA)$

**직교 행렬**:
- $Q^{-1} = Q^\top$
- $\det(Q) = \pm 1$
- $\|Q\mathbf{x}\| = \|\mathbf{x}\|$

**대칭 행렬**:
- 모든 고유값이 실수
- 직교 대각화 가능
- $A = QDQ^\top$

**SVD 관계**:
- $A^\top A = V\Sigma^2 V^\top$ (우특이벡터 = $A^\top A$의 고유벡터)
- $AA^\top = U\Sigma^2 U^\top$ (좌특이벡터 = $AA^\top$의 고유벡터)
- $\operatorname{rank}(A) = \#\{\sigma_i > 0\}$

---

## 실습 가이드

### PyTorch 주요 함수

```python
import torch

# 벡터/행렬 생성
A = torch.randn(3, 3)
I = torch.eye(3)

# 기본 연산
A_T = A.T                           # 전치
A_inv = torch.linalg.inv(A)         # 역행렬
det_A = torch.linalg.det(A)         # 행렬식
rank_A = torch.linalg.matrix_rank(A) # 랭크

# 고유값 분해
eigenvalues, eigenvectors = torch.linalg.eig(A)
eigenvalues, eigenvectors = torch.linalg.eigh(A)  # 대칭용

# SVD
U, S, Vh = torch.linalg.svd(A)
A_pinv = torch.linalg.pinv(A)      # Pseudo-inverse

# 선형 시스템
x = torch.linalg.solve(A, b)       # Ax = b
x = torch.linalg.lstsq(A, b)       # 최소제곱 해
```

### 학습 전략

1. **이론 → 예제 → 코드 순서로**
   - 각 노트의 정의와 정리 이해
   - 손으로 작은 예제 계산
   - PyTorch로 검증

2. **시각화 활용**
   - 2D 벡터 변환을 그려보기
   - 고유벡터 방향 표시
   - PCA 주성분 시각화

3. **연습 문제 필수**
   - 각 노트의 연습 문제 풀이
   - 증명을 따라가며 이해 심화

---

## 딥러닝 연결 포인트

### 어디에 쓰이나?

| 딥러닝 기법 | 선형대수 개념 | 관련 노트 |
|-----------|--------------|----------|
| 완전연결층 | 행렬 곱셈, 선형 변환 | [[행렬과 선형 변환]] |
| Attention | 내적, 행렬 곱 | [[벡터 공간과 기저]] |
| BatchNorm | 정규직교 기저 | [[행렬과 선형 변환]] |
| PCA, Autoencoder | SVD, 고유값 분해 | [[특잇값 분해]] |
| Gradient Descent | Hessian 고유값 | [[고유값과 고유벡터]] |
| Weight Initialization | 직교 행렬, 조건수 | [[행렬과 선형 변환]] |
| 추천 시스템 | Matrix Factorization | [[특잇값 분해]] |
| Spectral Norm | 최대 특이값 | [[특잇값 분해]] |

---

## 참고 자료

### 교과서
- Gilbert Strang, *Introduction to Linear Algebra*, 5th ed.
- David C. Lay, *Linear Algebra and Its Applications*
- Sheldon Axler, *Linear Algebra Done Right*
- Carl D. Meyer, *Matrix Analysis and Applied Linear Algebra*

### 온라인 강의
- MIT OCW 18.06 Linear Algebra (Gilbert Strang)
- 3Blue1Brown, *Essence of Linear Algebra* (YouTube)
- Khan Academy Linear Algebra

### 딥러닝 관점
- Ian Goodfellow et al., *Deep Learning*, Chapter 2
- "Matrix Calculus for Deep Learning" (Stanford CS231n)

---

## 다음 학습

선형대수 기초를 마쳤다면:

1. **미적분으로 확장**
   - [[Math/Calculus/미분과 미분법|미분과 미적분]]
   - Jacobian, Hessian과 선형대수의 연결

2. **최적화로 연결**
   - [[Math/Optimization/최적화 기초|최적화 기초]]
   - Gradient descent와 Hessian

3. **실전 응용**
   - [[Operators/선형 연산자|선형 연산자]]
   - [[Operators/행렬 분해|행렬 분해 모음]]

4. **상위 개념**
   - [[AI 기초 개요|AI 기초 전체 로드맵]]
