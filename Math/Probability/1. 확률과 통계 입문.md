## TL;DR
- 확률과 통계는 **불확실성을 정량화**하고 **데이터로부터 추론**하는 수학적 도구로, 딥러닝 전반에 필수적이다.
- 이 노트는 확률통계 전체 내용의 허브로, 확률 기초 → 통계 추정 → 정보 이론 순서로 학습하는 로드맵을 제공한다.
- 각 주제별 상세 노트로 연결하여 손실 함수, 베이지안 추론, 생성 모델을 체계적으로 이해할 수 있다.

---

## 학습 로드맵

| 순서 | 주제 | 핵심 질문 | 바로 가기 |
|------|------|-----------|-----------|
| 1 | 확률 기초 | 확률 공간, 분포, 기댓값, 베이즈 정리란? | [[확률 기초]] |
| 2 | 통계 추정 | MLE, MAP, 신뢰구간, 가설 검정은? | [[통계적 추론]] |
| 3 | 정보 이론 | 엔트로피, KL 발산, Mutual Information은? | [[정보 이론]] |

---

## 1. 확률 기초

### 핵심 개념
- **확률 공간**: $(\Omega, \mathcal{F}, P)$ - 공리적 정의
- **확률 변수**: $X: \Omega \rightarrow \mathbb{R}$
- **분포**: PMF (이산), PDF (연속), CDF
- **기댓값과 분산**: $\mathbb{E}[X]$, $\operatorname{Var}(X)$, $\operatorname{Cov}(X, Y)$
- **조건부 확률**: $P(A|B) = P(A \cap B) / P(B)$
- **베이즈 정리**: $P(A|B) = \frac{P(B|A)P(A)}{P(B)}$

### 주요 분포
**이산**:
- 베르누이, 이항, 포아송

**연속**:
- 균등, 정규, 지수

**다변량**:
- 다변량 정규분포 $N(\boldsymbol{\mu}, \Sigma)$

### 왜 중요한가?
- 베이즈 정리 → 사전/사후 분포, MAP 추정
- 다변량 정규 → VAE latent space, Gaussian process
- 조건부 확률 → 조건부 생성 모델

### 상세 학습
👉 [[확률 기초|확률 기초 상세 노트]]

**주요 정리**:
- 베이즈 정리: posterior ∝ likelihood × prior
- $\operatorname{Cov}(X, Y) = \mathbb{E}[XY] - \mathbb{E}[X]\mathbb{E}[Y]$

---

## 2. 통계 추정과 추론

### 핵심 개념
- **표본 통계량**: $\bar{X}$, $S^2$
- **중심극한정리**: $\bar{X}_n \xrightarrow{d} N(\mu, \sigma^2/n)$
- **MLE**: $\hat{\theta}_{\text{MLE}} = \arg\max L(\theta; \mathbf{x})$
- **MAP**: $\hat{\theta}_{\text{MAP}} = \arg\max [L(\theta) + \log p(\theta)]$
- **신뢰구간**: $P(\theta \in [L, U]) = 1 - \alpha$
- **가설 검정**: $H_0$ vs $H_1$, $p$-value

### 왜 중요한가?
- MLE = 신경망 학습 (negative log-likelihood 최소화)
- MAP = MLE + regularization (weight decay)
- 신뢰구간 → 모델 불확실성 정량화
- 가설 검정 → A/B 테스트, 성능 비교

### 상세 학습
👉 [[통계적 추론|통계 추정 상세 노트]]

**주요 정리**:
- 중심극한정리: 표본 평균은 근사적으로 정규분포
- Cramér-Rao: $\operatorname{Var}(\hat{\theta}) \geq 1/(nI(\theta))$

---

## 3. 정보 이론

### 핵심 개념
- **엔트로피**: $H(X) = -\sum p(x) \log p(x)$ (불확실성)
- **교차 엔트로피**: $H(p, q) = -\sum p(x) \log q(x)$ (분류 손실)
- **KL 발산**: $D_{\text{KL}}(p \| q) = \sum p(x) \log \frac{p(x)}{q(x)}$ (분포 차이)
- **Mutual Information**: $I(X; Y) = H(X) - H(X|Y)$ (공유 정보)
- **조건부 엔트로피**: $H(X|Y)$

### 왜 중요한가?
- Cross-Entropy Loss = 표준 분류 손실
- KL 발산 → VAE 정규화 항, distillation
- Mutual Information → 표현 학습, contrastive learning
- 정보 병목 → 일반화 이론

### 상세 학습
👉 [[정보 이론|정보 이론 상세 노트]]

**주요 정리**:
- $H(p, q) = H(p) + D_{\text{KL}}(p \| q)$
- $I(X; Y) = D_{\text{KL}}(p(x,y) \| p(x)p(y))$

---

## 주제별 연결

### 개념 간 관계도

```
확률 공간 ──────> 확률 변수 ──────> 분포 (PMF/PDF)
    │                                   │
    ▼                                   ▼
조건부 확률 ───> 베이즈 정리 ───> 사전/사후분포
    │                                   │
    ▼                                   ▼
표본 추출 ──────> 통계량 ──────> 추정 (MLE/MAP)
    │                                   │
    ▼                                   ▼
중심극한정리 ───> 신뢰구간/검정 ───> 추론
                                        │
                ┌───────────────────────┘
                ▼
        정보 이론 ───> 엔트로피
                │
        ┌───────┼───────┐
        ▼       ▼       ▼
       KL    Cross-   Mutual
            Entropy  Information
                │
        ┌───────┼───────┐
        ▼       ▼       ▼
      Loss    VAE   Contrastive
     Function      Learning
```

### 딥러닝 손실 함수 연결

| 손실 함수 | 확률/정보 이론 | 수식 |
|----------|--------------|------|
| **Cross-Entropy** | 교차 엔트로피 | $-\sum y \log \hat{y}$ |
| **NLL (Negative Log-Likelihood)** | MLE | $-\sum \log p(y_i \mid x_i; \theta)$ |
| **KL Loss** | KL 발산 | $\sum p \log(p/q)$ |
| **MSE** | 정규분포 MLE | $(y - \hat{y})^2$ |
| **BCE (Binary Cross-Entropy)** | 베르누이 MLE | $-[y \log \hat{y} + (1-y) \log(1-\hat{y})]$ |

---

## 빠른 참조

### 자주 쓰는 공식

**확률**:
- 베이즈: $P(A|B) = \frac{P(B|A)P(A)}{P(B)}$
- 전확률: $P(A) = \sum_i P(A|B_i)P(B_i)$
- 분산 분해: $\operatorname{Var}(X) = \mathbb{E}[X^2] - (\mathbb{E}[X])^2$

**주요 분포**:
- 정규: $N(\mu, \sigma^2)$, PDF = $\frac{1}{\sqrt{2\pi\sigma^2}} e^{-(x-\mu)^2/(2\sigma^2)}$
- 베르누이: $\mathbb{E}[X] = p$, $\operatorname{Var}(X) = p(1-p)$

**통계**:
- MLE: $\arg\max_{\theta} \prod p(x_i; \theta)$
- 표본 평균 분산: $\operatorname{Var}(\bar{X}) = \sigma^2/n$
- 95% CI: $\bar{x} \pm 1.96\sigma/\sqrt{n}$

**정보 이론**:
- 엔트로피: $H(X) = -\sum p \log p$
- KL: $D_{\text{KL}}(p \| q) = \sum p \log(p/q)$
- MI: $I(X; Y) = H(X) - H(X|Y)$

---

## 실습 가이드

### PyTorch 주요 함수

```python
import torch
from torch.distributions import *

# 분포 생성
normal = Normal(loc=0.0, scale=1.0)
bernoulli = Bernoulli(probs=0.5)
categorical = Categorical(probs=torch.tensor([0.25, 0.75]))

# 샘플링
samples = normal.sample((1000,))

# 로그 확률
log_prob = normal.log_prob(samples)

# KL 발산
from torch.distributions import kl_divergence
p = Normal(0.0, 1.0)
q = Normal(1.0, 2.0)
kl = kl_divergence(p, q)

# Cross-Entropy Loss
import torch.nn.functional as F
loss = F.cross_entropy(logits, targets)

# BCE Loss
bce = F.binary_cross_entropy(predictions, labels)
```

### 학습 전략

1. **확률 기초부터**
   - 베이즈 정리 완벽 이해
   - 주요 분포의 특성 암기
   - 조건부 확률 연습

2. **통계 추정**
   - MLE 손계산 연습
   - 중심극한정리 시뮬레이션
   - 신뢰구간 직접 구성

3. **정보 이론**
   - 엔트로피 계산 반복
   - KL 발산 비대칭성 체감
   - Cross-Entropy Loss 유도

4. **딥러닝 연결**
   - 각 손실 함수의 확률적 해석
   - MAP = MLE + regularization 이해
   - VAE, GAN 등의 이론적 토대

---

## 딥러닝 연결 포인트

### 어디에 쓰이나?

| 딥러닝 개념 | 확률/통계 도구 | 관련 노트 |
|-----------|--------------|----------|
| 분류 손실 | Cross-Entropy | [[정보 이론]] |
| 회귀 손실 (MSE) | 정규분포 MLE | [[통계적 추론]] |
| Weight decay | MAP (정규 사전분포) | [[통계적 추론]] |
| Dropout | 베르누이 샘플링 | [[확률 기초]] |
| Batch Normalization | 표본 평균/분산 | [[통계적 추론]] |
| VAE | KL 발산, 베이즈 추론 | [[정보 이론]] |
| GAN | Jensen-Shannon 발산 | [[정보 이론]] |
| Contrastive Learning | Mutual Information | [[정보 이론]] |
| Bayesian Deep Learning | 베이즈 정리, MAP | [[통계적 추론]] |

---

## 참고 자료

### 교과서
- Sheldon Ross, *A First Course in Probability*
- Casella & Berger, *Statistical Inference*
- Cover & Thomas, *Elements of Information Theory*
- Kevin P. Murphy, *Probabilistic Machine Learning*

### 온라인 강의
- MIT OCW 6.041 Probabilistic Systems Analysis
- Stanford CS229 Probability Review
- 3Blue1Brown, Probability Series

### 딥러닝 관점
- Ian Goodfellow et al., *Deep Learning*, Chapter 3
- Christopher Bishop, *Pattern Recognition and Machine Learning*

---

## 다음 학습

확률통계 기초를 마쳤다면:

1. **머신러닝으로 연결**
   - [[ML Foundations/손실 함수와 평가 지표|손실 함수와 평가]]
   - [[ML Foundations/학습 패러다임|학습 패러다임]]

2. **최적화와 융합**
   - [[Math/Optimization/최적화 기초|최적화 기초]]
   - MLE/MAP와 gradient descent

3. **고급 주제**
   - [[ML Foundations/정규화와 일반화|정규화와 일반화]]
   - Bayesian Deep Learning

4. **상위 개념**
   - [[AI 기초 개요|AI 기초 전체 로드맵]]
