## TL;DR
- 미분은 "입력값을 아주 조금 바꿨을 때 함수 값이 어떻게 변하는가"를 정량화하는 도구로, 딥러닝에서 손실 함수를 최소화하기 위한 **gradient descent**의 핵심 재료다.
- 이 노트는 미적분 전체 내용의 허브로, 단변수 → 다변수 → 자동 미분 → 최적화 순서로 학습하는 로드맵을 제공한다.
- 각 주제별 상세 노트로 연결하여 체계적으로 backpropagation과 최적화 이론을 이해할 수 있다.

---

## 학습 로드맵

| 순서  | 주제       | 핵심 질문                                     | 바로 가기              |
| --- | -------- | ----------------------------------------- | ------------------ |
| 1   | 단변수 미분   | 극한, 도함수, 극값은 어떻게 정의하는가?                   | [[2. 단변수 미적분]]     |
| 2   | 다변수 미분   | 편미분, gradient, Jacobian, Hessian이란?       | [[3. 다변수 미적분]]     |
| 3   | 자동 미분    | Chain rule을 어떻게 자동화하는가? Backpropagation은? | [[5. 자동 미분]]       |
| 4   | 테일러와 최적화 | 함수 근사와 최적성 조건은? Flat/Sharp minima란?       | [[4. 테일러 급수와 최적화]] |

---

## 1. 단변수 미분

### 핵심 개념
- **극한**: $\lim_{x \to a} f(x) = L$ (ε-δ 정의)
- **도함수**: $f'(x) = \lim_{h \to 0} \frac{f(x+h) - f(x)}{h}$
- **미분 규칙**: 곱셈, 몫, chain rule
- **극값**: 1차/2차 도함수 판정법
- **평균값 정리**: 평균 변화율 = 어딘가의 순간 변화율

### 왜 중요한가?
- 1차원 직관이 다변수 이해의 기초
- Chain rule이 backpropagation의 출발점
- 극값 조건이 최적화 이론의 토대

### 상세 학습
👉 [[2. 단변수 미적분|단변수 미적분 상세 노트]]

**주요 정리**:
- Chain rule: $(f \circ g)' = f'(g(x)) \cdot g'(x)$
- 평균값 정리: $f'(c) = \frac{f(b) - f(a)}{b - a}$ for some $c \in (a, b)$

---

## 2. 다변수 미분

### 핵심 개념
- **편미분**: $\frac{\partial f}{\partial x_i}$ (다른 변수 고정)
- **Gradient**: $\nabla f = [\frac{\partial f}{\partial x_1}, \ldots, \frac{\partial f}{\partial x_n}]^\top$ (가장 빠른 증가 방향)
- **Jacobian**: 벡터 함수의 미분 행렬 ($m \times n$)
- **Hessian**: 2차 편미분 행렬 (곡률 정보)
- **Chain rule**: $\frac{\partial \mathbf{z}}{\partial \mathbf{x}} = \frac{\partial \mathbf{z}}{\partial \mathbf{y}} \frac{\partial \mathbf{y}}{\partial \mathbf{x}}$

### 왜 중요한가?
- Gradient descent: $\theta_{t+1} = \theta_t - \eta \nabla L$
- Hessian으로 곡률 분석 (Newton 방법, 조건수)
- Jacobian이 backpropagation의 수학적 표현

### 상세 학습
👉 [[3. 다변수 미적분|다변수 미적분 상세 노트]]

**주요 정리**:
- 방향 미분: $D_{\mathbf{u}} f = \nabla f \cdot \mathbf{u}$
- Schwarz: $\frac{\partial^2 f}{\partial x \partial y} = \frac{\partial^2 f}{\partial y \partial x}$ (Hessian 대칭)

---

## 3. 자동 미분과 Backpropagation

### 핵심 개념
- **계산 그래프**: 연산을 노드, 데이터를 엣지로 표현
- **Forward mode**: Jacobian-vector product (입력 → 출력)
- **Reverse mode**: Vector-Jacobian product (출력 → 입력)
- **Backpropagation**: Reverse mode의 특수 케이스 (스칼라 출력)
- **PyTorch autograd**: `requires_grad`, `backward()`

### 왜 중요한가?
- 딥러닝 학습의 핵심 메커니즘
- 수백만 파라미터의 gradient를 효율적으로 계산
- Forward: $O(n)$, Backward: $O(n)$ (한 번에 모든 gradient!)

### 상세 학습
👉 [[5. 자동 미분|자동 미분 상세 노트]]

**주요 알고리즘**:
```
Forward: x → v₁ → v₂ → ... → y
Backward: ȳ=1 → v̄ₙ → v̄ₙ₋₁ → ... → x̄
```

---

## 4. 테일러 전개와 최적화

### 핵심 개념
- **1차 테일러**: $f(\mathbf{x} + \Delta\mathbf{x}) \approx f(\mathbf{x}) + \nabla f^\top \Delta\mathbf{x}$
- **2차 테일러**: $+ \frac{1}{2}\Delta\mathbf{x}^\top H \Delta\mathbf{x}$
- **최적성 조건**: 1차 필요 ($\nabla f = 0$), 2차 충분 ($H \succ 0$)
- **Newton 방법**: $\mathbf{x}_{k+1} = \mathbf{x}_k - H^{-1} \nabla f$
- **Flat/Sharp minima**: Hessian 고유값의 크기

### 왜 중요한가?
- Gradient descent의 수렴성 이론적 근거
- Newton, Quasi-Newton 최적화 알고리즘
- Flat minima가 일반화에 유리 (SAM 등)

### 상세 학습
👉 [[4. 테일러 급수와 최적화|테일러와 최적화 상세 노트]]

**주요 정리**:
- 2차 충분조건: $\nabla f = 0$, $H \succ 0$ → strict local min
- Sharpness: $\max_{\|\epsilon\| \leq \rho} f(\mathbf{x}^* + \epsilon) - f(\mathbf{x}^*)$

---

## 주제별 연결

### 개념 간 관계도

```
단변수 미분 ──────> 극한, 도함수 정의
    │                     │
    ▼                     ▼
Chain Rule ────> 다변수 확장 ────> Gradient, Jacobian
    │                                    │
    ▼                                    ▼
계산 그래프 ────> Backpropagation ◄──── Reverse Mode
    │                     │
    ▼                     ▼
자동 미분 ────────> PyTorch Autograd
                          │
                          ▼
            테일러 전개 ────> 1차 근사 (GD)
                          │
                          ▼
                    2차 근사 (Newton)
                          │
                          ▼
                  최적성 조건, Hessian
```

### 미분 도구 비교

| 방법 | 정확도 | 속도 | 메모리 | 용도 |
|------|--------|------|--------|------|
| **수치 미분** | 근사 | 느림 $O(n)$ | 적음 | Gradient checking |
| **기호 미분** | 정확 | 표현식 폭발 | 많음 | 수식 유도 |
| **자동 미분 (Forward)** | 정확 | $O(n)$ per input | 적음 | $n \ll m$ |
| **자동 미분 (Reverse)** | 정확 | $O(1)$ for all | 중간 | 딥러닝 ($m=1$) |

---

## 빠른 참조

### 자주 쓰는 미분 공식

**단변수**:
- $(x^n)' = nx^{n-1}$
- $(e^x)' = e^x$
- $(\ln x)' = 1/x$
- $(\sin x)' = \cos x$
- $(f \cdot g)' = f'g + fg'$
- $(f/g)' = (f'g - fg')/g^2$
- $(f \circ g)' = f'(g) \cdot g'$

**다변수**:
- Gradient: $\nabla f = [\frac{\partial f}{\partial x_1}, \ldots, \frac{\partial f}{\partial x_n}]^\top$
- 방향 미분: $D_{\mathbf{u}} f = \nabla f \cdot \mathbf{u}$
- Chain: $\frac{\partial z}{\partial x} = \frac{\partial z}{\partial y} \frac{\partial y}{\partial x}$

**행렬 미분** (자주 쓰는 것만):
- $\frac{\partial}{\partial \mathbf{x}}(\mathbf{a}^\top \mathbf{x}) = \mathbf{a}$
- $\frac{\partial}{\partial \mathbf{x}}(\mathbf{x}^\top A \mathbf{x}) = (A + A^\top)\mathbf{x}$
- $\frac{\partial}{\partial X}(\text{tr}(AX)) = A^\top$

---

## 실습 가이드

### PyTorch 주요 함수

```python
import torch

# 기본 설정
x = torch.tensor([1.0, 2.0], requires_grad=True)
y = (x ** 2).sum()

# Gradient 계산
y.backward()
print(x.grad)  # dy/dx

# Jacobian
from torch.autograd.functional import jacobian
J = jacobian(lambda x: x**2, x)

# Hessian
from torch.autograd.functional import hessian
H = hessian(lambda x: (x**2).sum(), x)

# 고차 도함수
x = torch.tensor(2.0, requires_grad=True)
y = x ** 3
dy_dx = torch.autograd.grad(y, x, create_graph=True)[0]
d2y_dx2 = torch.autograd.grad(dy_dx, x)[0]
```

### 학습 전략

1. **단변수부터 시작**
   - 극한, 도함수 정의 확실히
   - Chain rule 완벽히 이해
   - 극값 판별 연습

2. **다변수로 확장**
   - 편미분과 gradient 구별
   - Jacobian/Hessian 계산 연습
   - 2x2, 3x3부터 손으로 계산

3. **자동 미분 실습**
   - 작은 계산 그래프 손으로 그리기
   - Forward/backward pass 직접 계산
   - PyTorch로 검증

4. **최적화 연결**
   - Gradient descent 직접 구현
   - Learning rate와 수렴 관계 관찰
   - Newton vs GD 비교 실험

---

## 딥러닝 연결 포인트

### 어디에 쓰이나?

| 딥러닝 개념 | 미적분 도구 | 관련 노트 |
|-----------|-----------|----------|
| Loss function | 다변수 함수 | [[3. 다변수 미적분]] |
| Gradient descent | Gradient, 1차 테일러 | [[4. 테일러 급수와 최적화]] |
| Backpropagation | Chain rule, Reverse mode | [[5. 자동 미분]] |
| Learning rate | 테일러 근사 범위 | [[4. 테일러 급수와 최적화]] |
| Newton, L-BFGS | Hessian, 2차 테일러 | [[4. 테일러 급수와 최적화]] |
| Weight decay | Gradient 수정 | [[3. 다변수 미적분]] |
| Batch size | Gradient 분산 | [[3. 다변수 미적분]] |
| Activation function | Chain rule 적용 | [[5. 자동 미분]] |
| Skip connections | Gradient flow | [[5. 자동 미분]] |

---

## 참고 자료

### 교과서
- James Stewart, *Calculus*, 8th ed.
- Tom M. Apostol, *Calculus*, Volume 1-2
- Michael Spivak, *Calculus*

### 온라인 강의
- MIT OCW 18.01 Single Variable Calculus
- MIT OCW 18.02 Multivariable Calculus
- 3Blue1Brown, *Essence of Calculus* (YouTube)

### 딥러닝 관점
- Ian Goodfellow et al., *Deep Learning*, Chapter 4
- CS231n: "Backpropagation, Intuitions"
- Griewank & Walther, *Evaluating Derivatives*

---

## 다음 학습

미적분 기초를 마쳤다면:

1. **최적화로 연결**
   - [[Math/Optimization/1. 최적화 기초|최적화 기초]]
   - Gradient descent, Newton, Quasi-Newton

2. **선형대수와 융합**
   - [[Math/Linear Algebra/4. 고유값과 고유벡터|고유값]] (Hessian 분석)
   - [[Math/Linear Algebra/5. 특잇값 분해|SVD]] (Jacobian 분해)

3. **실전 응용**
   - [[ML Foundations/3. 머신러닝 최적화|ML에서의 최적화]]
   - [[ML Foundations/2. 손실 함수와 평가 지표|손실 함수와 평가]]

4. **상위 개념**
   - [[AI 기초 개요|AI 기초 전체 로드맵]]
